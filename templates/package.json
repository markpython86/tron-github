{
  "name": "tron-github-project",
  "version": "1.0.0",
  "description": "Tron Light Cycles game on GitHub contribution grid",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "gh-pages": "^6.1.0"
  }
}
